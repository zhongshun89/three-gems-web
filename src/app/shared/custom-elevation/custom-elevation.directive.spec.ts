import { CustomElevationDirective } from './custom-elevation.directive';

describe('CustomElevationDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomElevationDirective();
    expect(directive).toBeTruthy();
  });
});
